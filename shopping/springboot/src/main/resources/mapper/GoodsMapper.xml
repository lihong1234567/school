<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.GoodsDao">

    <select id="findBySearch" resultType="com.example.entity.Goods">
        select `goods`.*,
               `type`.`name` as type from `goods`
		left join `type` on `goods`.`typeId` = `type`.id

        where 1 = 1
		<if test="search != null and search.name != null and search.name != ''">
			and `goods`.`name` like concat ('%', #{search.name}, '%')
		</if>
        <if test="search != null and search.userName != null and search.userName != ''">
            and `goods`.`userName` = #{search.userName}
        </if>
        <if test="search != null and search.typeId != null and search.typeIs != ''">
            and `goods`.`typeId` like concat ('%', #{search.typeId}, '%')
        </if>

    </select>

</mapper>